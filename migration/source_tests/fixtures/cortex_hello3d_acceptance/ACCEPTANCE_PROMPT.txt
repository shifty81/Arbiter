Finish this project.

It should be a small Rust/wgpu desktop application that opens a working window, renders something simple, and displays the current local time in the window title, updating once per second.

Inspect the existing project first. Preserve wgpu where appropriate.

Repair the project, run its highest-authority project checkpoint or quality gate, fix failures until it passes, build it, launch the actual built executable, verify it remains running and behaves as requested, and only claim completion after the project is verified.

Do not stop after explaining errors. Complete the work autonomously.

Use the project's authoritative source and dependency-grounding systems rather than guessing external crate APIs. If Ground Before Write requires dependency grounding, resolve all required dependency packages as one bounded controller-owned recovery operation when possible, then continue the blocked mutation automatically.

Do not ask me to make edits, run commands, approve intermediate repairs, or perform build steps that Cortex can perform itself.

Keep the operation running through inspection, mutation, verification, build, launch, and runtime verification unless a genuine unrecoverable failure occurs.
