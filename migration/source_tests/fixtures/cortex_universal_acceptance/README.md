# Cortex Universal Acceptance Fixtures

These prompts are controller/adapter acceptance fixtures, not model benchmark answers.

- `cpp_window`: proves a non-Rust project can be profiled, compiled, launched, observed and closed normally.
- `rust_api_mismatch`: proves exact dependency grounding and reconstruction prevent mixed-version API hallucination loops.
- `image_reference`: proves typed workflow/model compatibility, provenance and vision/output-contract review.

Success must come from deterministic tool/build/runtime/artifact evidence. A model's prose claim is never sufficient.
