# O2D-R051N5 — Context / Vault / Memory Split

`cortex_context` now owns metadata inventory and bounded incremental file fingerprints. `cortex_workspace` remains safe filesystem/workspace authority. Lexical ProjectIndex moved into `cortex_vault`; `open2d_cortex_memory` is compatibility-only.
