# O2D-R051N2B — Open2D Tool Extension Extraction

`cortex_tools` now composes project-specific tools through `ToolExtension`. Open2D runtime launch/status/stop execution lives in `cortex_adapter_open2d`; the CLI injects it only for detected Open2D workspaces.
