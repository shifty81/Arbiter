# O2D-R051N7 — Providers / Process / Image / Capture

Generic authorities now own process execution, image artifact lifecycle, Windows capture, LM Studio, ComfyUI and provider capability registration. Historical `open2d_cortex_*` crates are thin re-export facades.
